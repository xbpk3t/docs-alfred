feed:
  timeout: 30 # 最多retry 6次，所以设置为30（第一次retry 5s，则第6次 = 5 * 2^5 = 160s，合计315s）
  maxTries: 6 # 最大retry次数
  feedLimit: 20 #

resend:
  token: # resend api token

newsletter:
  schedule: weekly # weekly(latest 7 days), daily(feeds of the day).
  isHideAuthorInTitle: false # default: true
