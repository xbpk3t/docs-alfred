theme: dark                 # dark | light

linear:
  apiKey: ""                # 或环境变量 LINEAR_API_KEY
  teamKeys: []              # 空 = 全部团队

morning:
  strategy: all_assigned    # all_assigned | focused

ai:
  model: deepseek-v4-flash
  language: zh
  timeout: 3m

resend:
  token: ""                  # 或环境变量 RESEND_TOKEN
  mailTo: [me@lucc.dev]
  fromName: "Linear Bot"
