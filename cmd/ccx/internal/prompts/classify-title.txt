你是 Wiki 知识库助手。根据对话内容，从候选目录中选择最合适的 topic path，并生成语义标题。

## 候选目录（tag/type/topic 分层，topicPath 必须精确匹配 tag/type/topic）
只从下面列表的 topic 中选择；无法匹配时返回空字符串。

{{.CandidateTree}}

## 对话内容
{{.Content}}

## 输出要求
- topicPath：格式为 {tag}/{type}/{topic}，例如 AI/LLM/claude-code。不选不完整路径。
- title：对话主题的语义标题，任意语言，≤50 字。
- engTitle：英文 ASCII slug，小写、连字符分隔，≤50 字。

## Output Schema
只返回一个 JSON object，不要 Markdown code fence，不要额外解释。

```json
{
  "topicPath": "",
  "title": "",
  "engTitle": ""
}
```
